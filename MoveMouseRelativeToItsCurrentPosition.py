#Move the mouse relative to its current position:
import pyautogui

# Move the mouse relative to its current position
offset_x = 100  # X-coordinate offset
offset_y = 50  # Y-coordinate offset

pyautogui.moveRel(offset_x, offset_y)